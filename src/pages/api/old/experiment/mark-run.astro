---
import ExperimentRemarks from "../../../../components/old/ExperimentRemarks.js";
import {currentExperimentId, currentRunId, updateRemark} from "../../../../lib/old/state.ts";
import {db, eq, experimentKnobData, experiments} from "astro:db";
import MarkingButton from "../../../../components/old/MarkingButton.js";
export const partial = true;
export const prerender = false;

if(Astro.request.method === 'POST' && currentRunId.value) {
  await db.insert(experimentKnobData).values({
    runId: currentRunId.value,
    type: "marking"
  })
}
---

<MarkingButton />
